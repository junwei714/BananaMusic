<?xml version="1.0" encoding="utf-8"?>
<com.facebook.shimmer.ShimmerFrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/shimmer_recommended"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:shimmer_auto_start="true"
    app:shimmer_base_color="@color/colorSurface"
    app:shimmer_highlighted_color="@color/colorPrimaryLight"
    app:shimmer_direction="left_to_right"
    app:shimmer_duration="800">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <!-- Shimmer items - note that we're now using views directly instead of including shimmer layouts -->
        <include layout="@layout/item_track_placeholder" />
        <include layout="@layout/item_track_placeholder" />
        <include layout="@layout/item_track_placeholder" />

    </LinearLayout>
</com.facebook.shimmer.ShimmerFrameLayout> 